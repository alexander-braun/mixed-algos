import { PaintObstacleDirective } from './paint-obstacle.directive';

describe('PaintObstacleDirective', () => {
  it('should create an instance', () => {
    const directive = new PaintObstacleDirective();
    expect(directive).toBeTruthy();
  });
});
